---

Compose replaces messy docker run commands with one YAML file

it handles networking automatically

YAML indentation errors and wrong service names are common mistakes

Debugging works the same - logs and exec

---

next we will learn how to build optimized docker images using multi stage builds