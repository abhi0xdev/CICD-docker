---
Always copy requirements.txt first to use catching

Always publish ports with -p and add EXPOSE for clarity and orchestration tools

Always use .dockerignore to keep images clean

and always tag your images properly -- never rely on latest in production.